<tr>
  <td><%= user.id %></td>
  <td><%= user.name %></td>
  <td><%= user.height %> / <%= user.weight %></td>
  <td><%= user.created_at %></td>
  <td><%= user.last_active_at %></td>
  <td>
    <%= link_to 'View', stylists_user_path(user) %><br>
    <%= link_to 'New message', new_stylists_user_message_path(user) %>
  </td>
</tr>
